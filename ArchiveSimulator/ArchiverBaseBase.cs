namespace ArchiveSimulator
{
    public class ArchiverBaseBase
    {
        public void ArchiveFiles(List documents)
        {
            foreach (Document doc in documents)
            {
                Document Archive = new Document();
                // Логика архивирования документа
            }
        }
    }
}